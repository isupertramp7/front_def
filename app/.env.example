# Copiar a .env.local para desarrollo local

# "true" = usa mocks locales en vez de API real
VITE_USE_MOCKS=true

# URL base de la API (default si no se define)
VITE_API_URL=https://api.gotest.app/v1
